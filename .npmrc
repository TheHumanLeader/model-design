fund=false
audit=true
save-exact=false

# Vue 3.6 is currently a prerelease. npm otherwise rejects plugin-vue's
# compatible ^3.2.25 peer range because prereleases are excluded by default.
legacy-peer-deps=true
